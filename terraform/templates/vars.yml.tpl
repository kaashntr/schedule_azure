# Application Tier
app_private_ip: ${app_private_ip}

# Backend Tier
backend_private_ip: ${backend_private_ip}

# Proxy Tier
proxy_private_ip: ${proxy_private_ip}
proxy_public_ip: ${proxy_public_ip}

# Database Tier (PostgreSQL)
postgre_hostname: ${postgre_hostname}

# Cache Tier (Redis)
redis_private_ip: ${redis_private_ip}

# Bastion Host
bastion_public_ip: ${bastion_public_ip}